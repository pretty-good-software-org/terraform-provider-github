---
page_title: "github_organization_security_configuration_repositories (Resource) - GitHub"
subcategory: ""
description: |-
  Resource to manage the repositories attached to a GitHub code security configuration for an organization.
---

# github_organization_security_configuration_repositories (Resource)

Resource to manage the repositories attached to a GitHub code security configuration for an organization.

This resource authoritatively manages the repositories attached to one organization code security configuration. The attachment API processes changes asynchronously.

The token must have the organization `Administration` permission with write access. See the [GitHub REST API reference](https://docs.github.com/en/rest/code-security/configurations#attach-a-configuration-to-repositories).

## Example Usage

```terraform
resource "github_organization_security_configuration_repositories" "example" {
  configuration_id = github_organization_security_configuration.example.configuration_id
  repository_ids = [
    github_repository.first.repo_id,
    github_repository.second.repo_id,
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `configuration_id` (Number) Numeric ID of the code security configuration.
- `repository_ids` (Set of Number) Numeric IDs of the repositories that use the code security configuration.

### Read-Only

- `id` (String) The ID of this resource.

## Import

Repository attachments can be imported using the code security configuration ID.

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = github_organization_security_configuration_repositories.example
  id = "123456"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import github_organization_security_configuration_repositories.example 123456
```
